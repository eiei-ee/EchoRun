# 2026-09-24 源码整理与交付

本次交付包含此前尚未提交的异步影子挑战和整套视觉迭代。云函数独立提交；客户端、共享场景、美术资源及必要生成工具作为完整依赖一起提交。当前视觉方向为“层叠记忆”，角色为连续衣裤曲面的“城市记忆信使”。历史 `OrangeEcho` 命名继续用于道路、材质与角色绑定，不代表恢复橙色方案。

## 内容与重建入口

- `Tools/CloudFunctions/echo/`：版本化冻结影子、幂等结果、按影子分榜；服务端上下文身份、事务及最小榜单字段。部署及集合设置见该目录 README。
- `Assets/WeixinMiniGame/`：微信 SDK 适配、邀请、发布分享、榜单 UI、超时与降级；非微信环境保持单机。环境配置默认禁用，仅提交 example，本机实际云配置不入库。
- `Assets/Scripts/`：独立的异步运行权限与结果路径；冻结对手、禁止异步局学习和晋升自己的 identity。
- `Assets/Art/LayeredMemory/`、场景与城市资源：分层街区、拱桥轨道、材质及细节；离线 Blender 源和脚本分别在 `ArtSource/Blender/LayeredMemory/`、`Tools/Art/`。
- `MemoryCourier.fbx` 及运行时网格：原骨架和动作绑定、连续衣裤造型。生成和安装顺序见 `ArtSource/Blender/LayeredMemory/MemoryCourier.md`。历史基础套件的重建说明已标明最后必须恢复当前配色与角色。
- UI、音效反馈、字体子集与安全区适配；字体来源及子集校验信息同步更新。

## 本次提交前验证

- Node 云函数离线测试：31/31，通过，无真实云调用。
- 完整 EditMode 回归：867/867，通过，0 跳过，Tuanjie 2022.3.62t8。首次 866/867，失败来自减弱动态效果测试残留旧红橙色条件；改为验证阶段颜色区分并保留光效强度约束后全量复测通过。
- 字体：1,053 字符与固定 Unicode 清单相符；11 个微信层 C# 文件、285 个字符串检查无缺字。
- 交付资源引用检查：没有已选资源指向被排除的候选资产，没有新增资产缺少 `.meta`。
- 先前本轮人物 Windows 构建和横竖屏实际动作检查见 `2026-09-24-memory-courier-form.md`。本次整理只补充测试、文档、忽略规则及移除废弃候选安装入口，没有再次构建三平台产物。

本地日志和测试 XML 位于被忽略的 `TestResults/LayeredMemoryRunnerForm-20260924/`（最终全量结果 `12-delivery-all-editmode.log.xml`），不作为发布资源上传。历史文档中较早的本机日志路径只用于说明当时证据，不保证其他机器或后续清理后仍存在。

## 保留在本地的内容与待验收

不提交测试截图与构建输出、Blender 自动备份、旧候选模型和动作、营销草稿、演示压缩包、本地工作记录及个人工具配置；未删除这些文件。

源码推送不等于微信上线。最新整套美术的 WebGL/微信构建、手机性能与画面、双账号真实分享卡片全链路，以及正式 HTTPS 资源托管仍需单独验收。之前的三目标构建和单身份云验证仅代表对应历史版本，不能替代当前版本的手机验收。
